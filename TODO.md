# Task TODO (Core Spring MVC clone)

- [ ] Create fresh Maven project skeleton for classic Spring (no Spring Boot) inside this folder.
- [ ] Add core dependencies: spring-context, spring-webmvc, servlet-api, slf4j/logback.
- [ ] Configure `web.xml` + `DispatcherServlet` (classic WAR-style layout).
- [ ] Add minimal MVC example: Controller + ViewResolver + JSP view.
- [ ] Add directory structure docs for Core Spring MVC (src/main/java + webapp).
- [ ] Build and run instructions (mvn package, run with embedded servlet container instructions or deployment notes).

